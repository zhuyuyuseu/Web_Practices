/**
 * Array utils
 */
var util = function() {
  return {
    isArray: function(obj){
      if(Array.isArray === 'function'){
        return Array.isArray(obj);
      }else{
        return (obj instanceof Array);
      }
    }
  };
};

module.exports = util;
